version: "3"
services:
    mlops-app:
        build: .
        ports:
            - "8080:8080"
        environment:
            - MLFLOW_TRACKING_URI=sqlite:////app/mlflow.db
        network_mode: "host"  # Use host network mode